# Primary hosting for Gemini mode is Cloud Run (see deploy-cloud-run.yml).
# Keep this workflow for optional static hosting only — run manually when needed.
#
# If deploy fails with: branch "main" is not allowed to deploy to github-pages —
# Repo → Settings → Environments → github-pages → Deployment branches and tags:
# allow "main" (e.g. "Selected branches" → add main, or "All branches").
name: Deploy to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
